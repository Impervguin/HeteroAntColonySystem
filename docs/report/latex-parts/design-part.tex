\chapter{Конструкторская часть}

В конструкторской части будет разработан комбинированный метод решения задачи коммивояжёра, основанный на муравьином алгоритме с динамическим подбором гетерогенных параметров с использованием генетического алгоритма. Будут описаны общая структура метода, детальные этапы его работы, используемые структуры данных, а также архитектурное разбиение на модули. Для наглядности основные алгоритмические решения будут представлены в виде IDEF0-диаграмм и схем алгоритмов.

\section{Обобщённый генетический алгоритм}

Для разработки предлагаемого комбинированного метода, необходимо рассмотреть основные принципы генетического алгоритма. Это обусловлено тем, что предлагаемый метод использует генетический алгоритм как механизм адаптации гетерогенных параметров муравьёв.

Генетический алгоритм представляет собой метаэвристику, вдохновлённую принципами естественного отбора и эволюции в живой природе. Впервые подобный подход был предложен Джоном Холландом в 1975 году~\cite{genetic}. Основная идея заключается в моделировании биологического процесса эволюции: популяция потенциальных решений задачи (особей) развивается из поколения в поколение за счёт применения операторов отбора, скрещивания и мутации. При этом более приспособленные особи, то есть дающие лучшее значение целевой функции, имеют больше шансов передать свои характеристики потомкам. В результате последовательных итераций популяция в целом улучшается, приближаясь к оптимальному решению.


\subsection{Понятия}

В рамках генетического алгоритма используются следующие базовые понятия~\cite{genetic-engineer}:

Особь -- потенциальное решение задачи, представленное. Каждая особь обладает определённым значением приспособленности.

Хромосома -- структура данных, кодирующая решение. Она состоит из генов, расположенных в определённом порядке. В зависимости от задачи гены могут быть бинарными, целочисленными, вещественными или иметь другую природу.

Ген -- элементарная единица хромосомы, отвечающая за кодирование отдельного параметра решения. Совокупность генов полностью определяет характеристики особи.

Аллель -- конкретное значение гена.

Популяция -- множество особей, существующих в рамках одного поколения. Размер популяции является фиксированным параметром и определяет количество одновременно рассматриваемых решений.

Поколение -- одна итерация эволюционного процесса, в результате которой из текущей популяции формируется новая.

Фитнес-функция (функция приспособленности) -- критерий качества особи, который оценивает, насколько хорошо данное решение удовлетворяет поставленной задаче. Значение фитнес-функции определяет вероятность участия особи в размножении.

Операторы селекции, скрещивания, мутации -- операции применяемые к хромосомам для получения и/или изменения особей. 

\subsection{Схема генетического алгоритма}

Схема классического генетического алгоритма представлена на рисунке~\ref{fig:genetic}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.2\textwidth]{genetic/genetic}
	\caption{Схема генетического алгоритма}
	\label{fig:genetic}
\end{figure}


\textbf{Инициализация популяции}

Формируется начальная популяция заданного размера. Каждая особь создаётся случайным образом или некоторым заданным алгоритмом.

\textbf{Оценка приспособленности}

Для каждой особи вычисляется значение фитнес-функции. Полученные значения позволяют ранжировать особей по качеству и используются на этапе селекции.

\textbf{Селекция (отбор)}

Из текущей популяции выбираются особи, которые станут родителями для создания потомства.
Существуют различные алгоритмы селекции, отличающиеся случайностью и стойкостью выбора родителей. Алгоритм обычно устроен таким образом, чтобы лучшие особи имели больше шансов стать родителями, но при этом сохранялось определённое разнообразие для предотвращения преждевременной сходимости.

\textbf{Кроссинговер (скрещивание)}

Из отобранных родителей формируются пары, которые в результате применения оператора кроссинговера создают одну или две особи-потомка. В зависимости от реализации оператора может происходить обмен участками хромосом (аллелями), их математическая комбинация или иное преобразование, наследующее признаки родителей.

\textbf{Мутация}

Каждый ген полученных потомков с малой вероятностью подвергается случайному изменению. Мутация поддерживает генетическое разнообразие популяции и позволяет исследовать новые области пространства поиска, которые могли быть утеряны в процессе селекции и кроссинговера

\textbf{Проверка критерия остановки}

Эволюционный процесс повторяется до выполнения одного из критериев:
\begin{itemize}
	\item достижение максимального числа поколений;
	\item достижение требуемого значения фитнес-функции;
	\item отсутствие улучшений в течение заданного числа поколений (стагнация);
	\item превышение допустимого времени работы.
\end{itemize}

\textbf{Таким образом}, генетический алгоритм оперирует популяцией особей, каждая из которых кодирует потенциальное решение. Последовательное применение операторов селекции, кроссинговера и мутации позволяет эволюционировать популяции в направлении улучшения качества решений. В предлагаемом комбинированном методе генетический алгоритм планируется к использованию для адаптации параметров $\alpha$ и $\beta$ муравьёв.

\section{Описание предлагаемого метода}

\subsection{Требования к разрабатываемому методу}

К разрабатываемому методу предъявляются следующие функциональные требования:
\begin{itemize}
	\item возможность решения задачи коммивояжёра для полных взвешенных неориентированных графов;
	\item адаптация параметров муравьёв в процессе поиска.
\end{itemize}

и нефункциональные требования:
\begin{itemize}
	\item улучшение сходимости по сравнению с классическим муравьиным алгоритмом;
	\item лучшая устойчивость к выбору начальных параметров $\alpha$ и $\beta$ по сравнению с классическим муравьиным алгоритмом.
\end{itemize}

\subsection{Формализация метода}

На рисунке~\ref{fig:02A0} представлено верхнеуровневое разбиение метода на последовательность действий. Блоки A1-A3 являются частями муравьиного алгоритма, а блок A4 описывает динамическую адаптацию параметров муравьев.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{method/02\_A0}
	\caption{IDEF0 диаграмма метода 1-го уровня}
	\label{fig:02A0}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{method/03\_A2}
	\caption{IDEF0 диаграмма формирования путей муравьёв}
	\label{fig:03A2}
\end{figure}

Диаграмма декомпозиции блока A2 (рисунок~\ref{fig:03A2}) описывает процесс формирования путей муравьями, совпадающей с классическим муравьиным алгоритмом, с добавлением локальной оптимизации для избранных путей поколения.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{method/03\_A4}
	\caption{IDEF0 диаграмма формирования новой популяции}
	\label{fig:03A4}
\end{figure}

Диаграмма декомпозиции блока A4 (рисунок~\ref{fig:03A4}) описывает работу генетического алгоритма со всеми его классическими этапами, однако особями в данном методе являются не решения задачи, а муравьи, в частности два их индивидуальных параметра: $\alpha$ и $\beta$.

Таким образом, параметрами метода, задаваемыми до начала работы являются:
\begin{itemize}
	\item начальные значения $\alpha_0$ и $\beta_0$ -- коэффициенты первого поколения муравьёв;
	\item начальное количество феромона $\tau_0$ -- феромон, устанавливаемый для каждого ребра при инициализации карты;
	\item множитель феромона $Q$ -- определяет количество феромона распыляемую на единицу длины пути;
	\item коэффициент испарения $p$ -- определяет долю феромона, испаряющуюся в конце каждой итерации;
	\item количество итераций -- параметр критерия остановки, равняющийся количеству поколений муравьёв
	\item размер популяции $m$ -- количество муравьев, в одном поколении. Фактически, является количеством построенных решений в рамках одного поколения;
	\item период адаптации $w$ -- количество последовательных итераций, без обновления поколения; 
	\item количество родителей -- количество муравьев, отбираемого для скрещивания
	\item параметры алгоритмов скрещивания, мутации, селекции.
\end{itemize}

Таким образом, генетический алгоритм интегрируется как метаэвристика над муравьиным алгоритмом, которая динамически адаптирует параметры муравьев под конфигурацию графа и текущего уровня феромонов.

\section{Поиск муравьиным алгоритмом}

На основе представленной IDEF0-формализации и выделенных параметров метода ниже приводится детальное описание каждого этапа. Для каждого этапа описываются используемые алгоритмы, формулы и параметры, а также их обоснование с точки зрения цели разрабатываемого комбинированного подхода.

\subsection{Инициализация первого поколения}

\textbf{Инициализация феромонов}

Так же как и в классическом муравьином алгоритме на каждом ребре устанавливается в небольшое положительное значение уровня феромона: $$\tau_{ij}(0)=\tau_0$$

Это позволяет сгладить первую итерацию, увеличивая шансы на выбор муравьём более длинных участков графа, что увеличивает начальную область поиска и разнообразие решений.

\textbf{Формирование начальной популяции муравьёв}

Создаётся популяция размером $m$, где $m$ — количество муравьёв в одном поколении. Каждый муравей имеет индивидуальные $\alpha$ и $\beta$, равные некоторому установленному для всех значению по умолчанию.

\subsection{Формирование путей муравьиным алгоритмом}

Данный этап выполняется на каждой итерации для всех муравьёв, в результате чего находится множество и $m$ решений.

\textbf{Выбор начальной вершины}

Для каждого муравья начальная вершина выбирается случайным образом. Это позволяет исследовать различные маршруты и снижает влияние начального выбора на итоговый результат.

\textbf{Правило выбора очередной вершины}

Вероятностное правило выбора следующей вершины пути аналогично классическому варианту с поправкой на гетерогенность параметров: Пусть на итерации $k$ муравей $a$ находится в вершине $v_i$, а множество ещё не посещённых им вершин обозначается как $U_a(i)$. Тогда вероятность выбора вершины $v_j \in U_a(i)$ в качестве следующей задаётся формулой:

\begin{equation}
	P_{ij}^{(a)}(k) =
	\begin{cases}
		\displaystyle \frac{\tau_{ij}(k)^{\alpha_a} \cdot \eta_{ij}^{\beta_a}}
		{\sum\limits_{v_l \in U_a(i)} \tau_{il}(k)^{\alpha_a} \cdot \eta_{il}^{\beta_a}}, & \text{если } v_j \in U_a(i); \\
		0, & \text{иначе}, \\
	\end{cases}
\end{equation}

где
\begin{itemize}
	\item $\tau_{ij}(k)$ -- интенсивность феромона на ребре $e_{ij}$ на итерации $k$;
	\item $\eta_{ij} = \frac{1}{F(e_{ij})}$ -- эвристическая значимость ребра (обратная длина);
	\item $\alpha_a$ и $\beta_a$ -- индивидуальные параметры муравья aa, определяющие степень влияния феромона и эвристики соответственно.
\end{itemize}

\textbf{Вычисление длин маршрутов}

После того как все муравьи завершили построение своих маршрутов, происходит вычисление длины каждого цикла:

\begin{equation}
	L_a(k) = F(Y^k_a),
\end{equation}

где $Y^k_a$ -- цикл, построенный муравьём $a$.

\textbf{Локальная оптимизация}

Локальная оптимизация -- метод, улучшающий уже найденное решение. Применение локальной оптимизации позволяет повысить качество решений без значительного увеличения вычислительных затрат~\cite{mmas}. В качестве кандидатов на применение локальной оптимизации предлагается брать родителей, выбираемых при селекции, так как эти особи уже отобраны как лучшие для скрещивания.

Наиболее популярной группой алгоритмов локальной оптимизации для задачи коммивояжёра являются \texttt{k-opt} алгоритмы, которые выполняют перестановку \texttt{k} рёбер в маршруте, замыкая его заново. При увеличении \texttt{k} качество улучшения растёт, однако \texttt{k-opt} имеет экспоненциальную вычислительную сложность $O(n^k)$, где $n$ -- число вершин графа, что делает \texttt{k-opt} нецелесообразным при $k > 3$  для муравьиного алгоритма.


В качестве метода локальной оптимизации в данной работе предлагается использовать \texttt{2-opt} как наиболее сбалансированный по соотношению эффективности и вычислительных затрат~\cite{2-opt}.

Псевдокод алгоритма локальной оптимизации маршрута методом 2-opt~\cite{2-opt-algo} представлен в листинге~\ref{lst:2opt}.

\begin{lstlisting}[caption={Алгоритм 2-opt оптимизации маршрута}, label={lst:2opt}, numbers=left]
// маршрут - последовательность вершин в порядке обхода [v1, v2, ..., vn]
найдено_улучшение := ИСТИНА
Пока найдено_улучшение:
	найдено_улучшение := ЛОЖЬ
	Для i от 1 до n-1:
		Для j от i+1 до n:
            // проверка граничных условий
			Если i+1 >= n или j+1 >= n:
				продолжить
			Если i+1 = j или j+1 = i:
				продолжить
			// вычисляем изменение длины маршрута
			текущ_расст := длина(маршрут[i], маршрут[j]) + длина(маршрут[i+1], маршрут[j+1])
			новое_расст := длина(маршрут[i], маршрут[i+1]) - длина(маршрут[j], маршрут[j+1])
			дельта =  текущ_расст - новое_расст
			
			Если дельта < 0:
			// выполняем переворот участка между i+1 и j
			    переворот(маршрут, i+1, j)
			    найдено_улучшение := ИСТИНА
\end{lstlisting}

\subsection{Обновление феромона}

Обновление феромона в предлагаемом комбинированном методе проводится аналогично классическому муравьиному алгоритму в два этапа: испарение и добавление нового феромона.

\textbf{Испарение феромона}

На первом этапе происходит уменьшение концентрации феромона на всех рёбрах графа. Это моделирует естественный процесс рассеивания химического следа и позволяет алгоритму постепенно "забывать" устаревшую информацию, фокусируясь на более перспективных участках. Испарение выполняется по формуле:
\[
\tau_{ij}(t+1) = (1 - \rho) \cdot \tau_{ij}(t),
\]
где \( \rho \in (0, 1] \) -- коэффициент испарения, определяющий скорость уменьшения феромона; \( t \) -- номер текущей итерации.

\textbf{Добавление феромона}

После испарения каждый муравей \( a \) добавляет феромон на рёбра, вошедшие в построенный им маршрут \( Y_a^k \). Количество добавляемого феромона обратно пропорционально длине маршрута, что усиливает более короткие пути:
\[
\tau_{ij}(t+1) = \tau_{ij}(t1) + \sum_{a=1}^{m} \Delta\tau_{ij}^{(a)},
\]
\[
\Delta\tau_{ij}^{(a)} = 
\begin{cases}
	\dfrac{Q}{L_a}, & \text{если ребро } (i,j) \text{ входит в маршрут муравья } a, \\
	0, & \text{иначе},
\end{cases}
\]
где \( Q \) -- множитель феромона; \( L_a \) -- длина маршрута, построенного муравьём \( a \).


\subsection{Выбор значений параметров}

Как уже было описано выше, муравьиный алгоритм существенно зависит от параметров, определяющих его поведение и эффективность. В книге~\cite{dorigo-book} определили значения, дающие хороший результат на большом наборе входных данных:

\begin{itemize}
	\item $\alpha$ -- 1;
	\item $\beta$ -- 2-5;
	\item множитель феромона $Q$ -- 1;
	\item коэффициент испарения $\rho$ -- 0.3;
	\item количество муравьв $m$ -- $n$;
	\item начальный уровень феромона $\tau_0$ -- $\frac{m}{C_{nn}}$;
\end{itemize}

где $n$ -- число узлов в графе, $C_{nn}$ --длина маршрута, построенная некоторой элементарной эвристикой, например жадным алгоритмом. Указанные значения $\alpha$ и $\beta$ могут служить как начальные в предлагаемом методе, а остальные использоваться как референсные значения.

\section{Формирование новой популяции генетическим алгоритмом}

Формирование новой популяции является ключевым этапом, отличающим предлагаемый комбинированный метод от классического муравьиного алгоритма. Именно на этом этапе происходит адаптация гетерогенных параметров $\alpha_a$ и $\beta_a$ муравьёв под конкретную конфигурацию графа и текущую ситуацию поиска.

Новая популяция формируется из текущей с использованием генетического алгоритма и его классических: селекции, скрещивания и мутации. Процесс выполняется каждые $w$ итераций (период адаптации), что позволяет муравьям построить маршруты с текущими параметрами для оценки их приспособленности и эффективности.

В качестве особей генетического алгоритма выступают муравьи, а в качестве хромосомы -- вектор из двух вещественных чисел: параметров $\alpha_a$ и $\beta_a$. В качестве фитнес-функции муравья предлагается величина, обратная длине его пути:
$$f_a=\frac{1}{L_a},$$

где $L_a$ -- длина маршрута, построенного муравьём $a$.

\subsection{Алгоритмы селекции}

Селекция предназначена для отбора родителей, которые будут участвовать в скрещивании. В разрабатываемом методе предлагаются 3 основные стратегии селекции~\cite{genetic-engineer}.

\textbf{Элитарная селекция}

Данная стратегия является наиболее простой и прямолинейной -- в качестве родителей отбирается заданное количество особей с наилучшими показателями приспособленности (наименьшей длиной маршрута). Такой способ гарантирует сохранение лучших генетических характеристик, однако он уменьшает пространство поиска, так как теряется разнообразие популяции.

\textbf{Рулеточная селекция}

Данный метод основан вероятностном подходе. Вероятность выбора особи пропорциональна её приспособленности. Чем лучше особь (чем короче построенный ею маршрут), тем больше вероятность быть выбранной в качестве родителя. Соответственно вероятность выбора муравья $a$ как родителя равна: $$P(a)=\frac{f_a}{\sum_{j=1}^{m}f_j},$$

где $f_a=\frac{1}{L_a}$ -- приспособленность особи, а $m$ -- размер популяции.

Рулеточная селекция сохраняет разнообразие популяции, так как даже наихудшие особи имеют ненулевую вероятность быть выбранными, однако она чувствительна к масштабу значений фитнес-функции.

\textbf{Турнирная селекция}

Турнирный метод основан одновременно на случайности и выборе лучшего кандидата. Для каждого требуемого родителя случайным образом выбирается подмножество особей из популяции размером $k$ (размер турнира), после чего из этого подмножества выбирается особь с наилучшим показателем приспособленности.

Турнирная селекция устойчива к масштабу значений фитнес-функции и позволяет регулировать давление отбора через параметр $k$, при этом наиболее частыми и удачными являются значения $k=3$ и $k=5$~\cite{genetic-engineer}

\subsection{Алгоритмы скрещивания}

Скрещивание (кроссинговер) создаёт новых особей-потомков на основе генетической информации родителей. Так как хромосома в данном случае является вектором вещественных чисел, то наиболее рациональным будет использовании арифметических алгоритмов скрещивания. В разрабатываемом методе предлагается 3 часто используемых метода арифметического кроссинговера.

В рамках описания алгоритмов скрещивания, примем что уже выбраны некоторые родители \(a_1\) и \(a_2\) с параметрами \((\alpha_{a_1}, \beta_{a_1})\) и \((\alpha_{a_2}, \beta_{a_2})\) соответственно.

\textbf{Арифметический кроссовер}

Потомок \(a\) рождается как линейная комбинация родительских параметров:
\[
\alpha_a = \lambda \cdot \alpha_{a_1} + (1 - \lambda) \cdot \alpha_{a_2}, \quad \beta_c = \lambda \cdot \beta_{a_1} + (1 - \lambda) \cdot \beta_{a_2},
\]

Чаще всего берут $\lambda=0.5$, что позволяет равномерно учитывать влияние обоих выбранных родителей

\textbf{BLX-кроссовер (Blend Crossover)}

Данный оператор создаёт потомка в интервале, расширенном относительно родительских значений. 

Пусть \(\alpha_{\min} = \min(\alpha_{a_1}, \alpha_{a_2})\), \(\alpha_{\max} = \max(\alpha_{a_1}, \alpha_{a_2})\), а размах значений составляет \(I_\alpha = \alpha_{\max} - \alpha_{\min}\). Тогда значение $\alpha_a$ для потомка равномерно выбирается из интервала:

\[
\alpha_a \sim U\bigl[\alpha_{\min} - \gamma \cdot I_\alpha,\; \alpha_{\max} + \gamma \cdot I_\alpha\bigr],
\]
где $U[a,b]$ -- равномерная случайная величина на отрезке $[a, b]$, а $\gamma$ -- параметр BLX-кроссовера.

Аналогично для параметра $\beta$:
\[
\beta_a \sim U\bigl[\beta_{\min} - \gamma \cdot I_\beta,\; \beta_{\max} + \gamma \cdot I_\beta\bigr],
\]

При значении $\gamma$, равном 0, потомок выбирается строго между родительскими значениями. При положительном значении интервал расширяется за пределы родительских значений, что позволяет исследовать области, лежащие за пределами родительских характеристик. Согласно~\cite{genetic-engineer} при значении $\gamma=0.5$ данный вид кроссинговера работает эффективнее арифметических кроссоверов.

\textbf{SBX-кроссовер}

В отличие от предыдущих методов, в которых особь единолично хранило генетическую информацию, в данном методе её хранит пара, особей, которые по отдельности могут существенно расходится. Особенностью метода является то, что он имитирует точечный двоичный кроссовер для вещественных чисел и имеет следующие свойства:

\begin{itemize}
	\item средние арифметические значения фитнес-функций потомков и родителей равны;
	\item сохраняется внешнее распределение решений.
\end{itemize}

Для заданных родителей создаются два потомка, с параметрами:
\[
\alpha^{a}_1 = 0,5 \left[(\alpha_{a_1} + \alpha_{a_2}) - \gamma \cdot (\alpha_{a_2} - \alpha_{a_1} )\right], \quad \alpha^{a}_2 = 0,5 \left[(\alpha_{a_2} + \alpha_{a_1}) + \gamma \cdot (\alpha_{a_2} - \alpha_{a_1})\right],
\]
\[
\beta^{a}_1 = 0,5 \left[(\beta_{a_1} + \beta_{a_2}) - \gamma \cdot (\beta_{a_2} - \beta_{a_1} )\right], \quad \beta^{a}_2 = 0,5 \left[(\beta_{a_2} + \beta_{a_1}) + \gamma \cdot (\beta_{a_2} - \beta_{a_1})\right],
\]

$$
\gamma(u) = \begin{cases}
	(2u)^{\frac{1}{\eta + 1}}, & \text{при } u(0,1) < 0.5, \\
	(\frac{1}{2(1 - u)})^{\frac{1}{\eta + 1}}, & \text{при } u(0,1) >= 0.5,
\end{cases}
$$

где $u(0,1)$ -- равномерная случайная величина на отрезке $[0, 1]$, а  $\eta \in [2, 5]$ -- параметр разброса SBX, чем он больше, тем больше вероятность появления потомков с параметрами близкими к родительским. На некоторых конфигурациях генетического алгоритма данный алгоритм показывает большую эффективность относительно BLX~\cite{genetic-engineer}, поэтому имеет смысл его рассмотреть.

\subsection{Алгоритмы мутации}

Мутация вносит случайные изменения в параметры потомков, поддерживая генетическое разнообразие популяции и позволяя исследовать новые области пространства поиска. Стандартным способом мутации хромосом вещественных чисел, является прибавление к ним некоторого небольшого случайного вещественного, что имитирует мутацию в естественной среде~\cite{genetic-engineer}

\textbf{Равномерная мутация}

Наиболее классическим и простым вариантом вещественной мутации является равномерная мутация, при котором к текущему значению гена прибавляется равномерно распределённая случайная величина в заданном диапазоне. Соответственно в данном методе мутация имеет вид:

\[
\alpha' = \alpha + U[l,r], \quad \beta' = \beta + U[l,r],
\]

где $U[a,b]$ -- равномерная случайная величина на отрезке $[l, r]$, задаваемом как параметр алгоритма.

\textbf{Гауссовская мутация}

Арифметическая мутация является грубым оператором, так как она c равной вероятностью вносит изменения любой величины в пределах заданного интервала, что может приводить к чрезмерным отклонениям параметров и нарушению сходимости~\cite{genetic-engineer}. В рамках~\cite{hetero} предложили более естественный и плавный механизм мутации, показавшим себя более удачным выбором -- гауссовская мутация. В этом случае мутация имеет вид:

\[
\alpha' = \alpha + \mathcal{N}(\mu, \sigma), \quad \beta' = \beta + \mathcal{N}(\mu, \sigma),
\]

где \(\mathcal{N}(\mu, \sigma)\) -- нормально распределённая случайная величина с математическим ожиданием $\mu$ и стандартным отклонением \(\sigma\), задаваемым как параметр алгоритма.

\section{Описание структур данных}

Для реализации разрабатываемого комбинированного метода необходимо определить набор структур данных, обеспечивающих хранение и обработку информации о графе, феромонах, муравьях и популяции в целом. Ниже приведено описание каждой структуры и обоснование выбранных представлений.

\subsection{Граф}

Графы в решаемой задаче имеют следующие особенности:

\begin{itemize}
	\item графы полные, т.е. для каждой пары вершин существует ребро; 
	\item граф взвешенный -- требуется хранить не только факт связи, но и вес;
	\item граф не изменяется в процессе решения задачи;
	\item частые операции итераций по смежным вершинам -- при выборе следующей вершины муравьем;
\end{itemize}

Исходя из этого наиболее эффективной структурой данных для представления графа в данном методе является матрица смежности~\cite{algos}, так как она:

\begin{itemize}
	\item обеспечивает доступ к весу ребра между любыми двумя вершинами за время $O(1)$, что критически важно для вычисления вероятностей перехода и эвристической привлекательности;
	\item матрица занимает $n^2$ ячеек памяти, независимо от количества рёбер, в отличие от других представлений, зависящих от количества рёбер;
	\item итерация по всем смежным вершина возможно реализовать как итерацию по строке матрицы, без накладных расходов.
\end{itemize}

\subsection{Карта феромонов}

Карта феромонов служит для хранения концентрации феромона и фактически также является взвешенным графом. В отличие от входного графа, предполагает частые обновления весов (значений феромона) на отдельных ребрах.

Для представления карты феромонов также рационально выбрать матрицу смежности, так как она, помимо описанных выше особенностей, позволяет за O(1) менять значение на отдельно взятом ребре, что критично для данной структуры.

\subsection{Муравей}

Муравей является агентом, строящим маршрут. Каждый муравей обладает следующими данными:

\begin{itemize}
	\item $\alpha$ и $\beta$ -- вещественные числа, в разрабатываемом методе эти параметры гетерогенны поэтому хранятся индивидуально для каждого муравья;
	\item маршрут (путь) — последовательность вершин, отражающая порядок обхода городов;
	\item длина маршрута — сумма весов рёбер в маршруте;
	\item память (табу-список) — множество посещённых вершин, используемое для запрета повторного посещения;
\end{itemize}

Для представления муравья в памяти предполагается использование тип данных определяющих несколько полей в одну совокупность (структура, класс). 

Выбор структуры данных для представления маршрута должен исходит из следующих его особенностей:
\begin{itemize}
	\item маршрут имеет фиксированную длину, равную количеству вершин графа $n$;
	\item частое добавление вершин в конец маршрута в процессе построения;
	\item важно сохранение порядка элементов;
	\item редкое полное копирование маршрута (при сохранении лучшего решения);
	\item необходимость вычисления длины маршрута путём суммирования весов рёбер.
\end{itemize}

Исходя из этого, для представления маршрута предлагается использовать массив фиксированного размер $n$. Такой выбор обеспечивает:

\begin{itemize}
	\item сохранение порядка за счёт индексации массива;
	\item простоту копирования при необходимости сохранить лучший маршрут;
	\item компактное хранение (без накладных расходов на динамические структуры);
	\item возможность предварительного выделения памяти.
\end{itemize}

Табу-список используется на этапе выбора следующей вершины пути муравья и обладает следующими особенностями: 

\begin{itemize}
	\item частые операции проверки наличия элемента ($n^2$ операций для одного муравья);
	\item количество элементов фиксировано и равно $n$;
	\item операции добавления вершины в список выполняются после каждого перехода.
\end{itemize}

Для реализации табу-списка предлагается использовать \textbf{хеш-таблицу (множество)}~\cite{virt}. Такой подход обеспечивает:

\begin{itemize}
	\item проверку наличие вершины в списке за $O(1)$ ;
	\item добавление элемента за $O(1)$;
	\item возможность предварительного выделения структуры под заданное число элементов.
\end{itemize}

\subsection{Популяция муравьёв}

Популяция представляет собой совокупность муравьёв, действующих в рамках одного поколения. Основные особенности популяции:

\begin{itemize}
	\item количество муравьёв фиксированно;
	\item не важен порядок муравьев;
	\item важно возможность быстрой очистки структуры, перед новым заполнением;
	\item важна возможность быстрой сортировки по длине построенных маршрутов (для некоторых вариантов селекции).
\end{itemize}

Исходя из этого, для представления популяции предлагается использовать \textbf{массив (срез)} муравьёв фиксированного размера $m$. Такой выбор обеспечивает:

\begin{itemize}
	\item прямой доступ к любому муравью по индексу за O(1);
	\item возможность быстрой сортировки с использованием стандартных алгоритмов (например, быстрой сортировки хоара~\cite{virt} за $O(mlog(m))$);
	\item простоту очистки — достаточно перезаписать массив новыми данными или создать новый;
	\item компактное хранение без накладных расходов на указатели (в отличие от связных списков).
\end{itemize}

\subsection{Итоговый выбор структур данных}

Результаты выбора структур данных для реализации разрабатываемого метода сведены в таблицу~\ref{tbl:dschoice}.

\begin{longtable}{|p{0.25\textwidth}|p{0.3\textwidth}|p{0.35\textwidth}|}
	\caption{Выбор структур данных для реализации метода}\label{tbl:dschoice} \\
	\hline
	\textbf{Структура} & \textbf{Выбранное представление} & \textbf{Обоснование} \\
	\hline
	\endfirsthead
	\caption[]{Выбор структур данных для реализации метода (продолжение)} \\
	\hline
	\textbf{Структура} & \textbf{Выбранное представление} & \textbf{Обоснование} \\
	\hline
	\endhead
	\hline
	\endfoot
	\endlastfoot
	Граф & Матрица смежности & Доступ к весу ребра за $O(1)$ полный граф \\
	\hline
	Карта феромонов & Матрица феромонов (смежности)  & Доступ и обновление за $O(1)$, полный граф \\
	\hline
	Маршрут муравья & Массив фиксированного размера & Фиксированная длина, добавление в конец за $O(1)$, простота копирования \\
	\hline
	Табу-список & Хеш-таблица (множество) & Проверка и добавление за $O(1)$ \\
	\hline
	Популяция & Массив фиксированного размера  & Прямой доступ, быстрая сортировка, простота очистки \\
	\hline
\end{longtable}

\section{Архитектурное разбиение на модули}

Для разграничения зон ответственности и возможности гибкой конфигурации метода, предлагается архитектура с разбиением на отдельные модули и компоненты, описанная в UML диаграмме~\ref{fig:components}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{construct/Components}
	\caption{UML-диаграмма модулей и компонентов предлагаемого метода}
	\label{fig:components}
\end{figure}

\subsection{Модуль структуры данных}

Модуль структур данных отвечает за описание абстрактных типов данных и их реализацию, для использования в ядре метода. Данный модуль не содержит логики структур, а только предоставляет интерфейсы для доступа к данным.

Состоит из двух сущностей, реализующих соответствующие структуры данных:
\begin{itemize}
	\item граф;
	\item карта феромонов.
\end{itemize}

\subsection{Модуль ядро}

Модуль ядра реализует основную логику комбинированного метода и является центральным компонентом архитектуры. Он не зависит от конкретных реализаций стратегий, а определяет интерфейсы, что позволяет гибко конфигурировать поведение метода.

Состоит из двух основных компонентов:

\textbf{Колония муравьев}

\begin{itemize}
	\item содержит популяцию муравьёв, граф и карту феромонов;
	\item управляет итерационным процессом: построение маршрутов, обновление феромонов, эволюция популяции, сохранением лучшего пути;
	\item передает срезы информации в определенные моменты наблюдателям, для сбора информации о процессе выполнения;
	\item реализует критерии остановки алгоритма;
	\item определяет интерфейсы для стратегий генетического алгоритма и наблюдателей.
\end{itemize}


\textbf{Муравей-агент}

\begin{itemize}
	\item хранит индивидуальные параметры $\alpha$ и $\beta$;
	\item определяет алгоритм построение маршрута;
	\item определяет интерфейс метода локальной оптимизации и применяет его;
	\item вычисляет длину построенного маршрута.
\end{itemize}


Таким образом колония муравьев является управляющим элемент, задающим общие шаги метода, при этом построение пути и распыление феромона ложится на самих муравьев, а конкретные вариации генетических операторов легко подменяются за счёт определенных интерфейсов на этапе инициализации.

\subsection{Модуль стратегии}

Модуль стратегий содержит конкретные реализации интерфейсов, определённых в ядре. Вынесение стратегий в отдельный модуль реализует паттерн проектирования <<Стратегия>>~\cite{patterns}, который позволяет:

\begin{itemize}
	\item отделить алгоритмы от использующего их контекста (ядро метода);
	\item подменять алгоритмы на этапе конфигурации без изменения кода ядра;
	\item добавлять новые алгоритмы без модификации существующих компонентов.
\end{itemize}

Таким образом, все вариативные части метода (алгоритмы селекции, скрещивания, мутации, выбора пути, обновления феромонов, локальной оптимизации) вынесены в модуль стратегий. Ядро же содержит только неизменяемую логику работы колонии муравьёв — последовательность этапов и управление итерационным процессом.

\subsection{Модуль наблюдатели}

Модуль наблюдателей реализует шаблон проектирования <<Наблюдатель>>~\cite{patterns}, позволяя отслеживать состояние алгоритма в реальном времени без вмешательства в его логику. Все наблюдатели реализуют интерфейс, определённый в ядре, что позволяет легко модифицировать собираемую информацию.

Механизм работы модуля наблюдателей построен на следующих принципах:

\begin{itemize}
	\item ядро определяет интерфейс наблюдателя и передаваемую им информацию о текущем состоянии метода;
	\item при начальной конфигурации колония регистрирует список наблюдателей;
	\item после каждой итерации колония собирает текущее состояние и уведомляет всех зарегистрированных наблюдателей, передавая им срез текущего состояния (популяция муравьёв, карта феромонов, лучшее решение, номер итерации и т.д.).
\end{itemize}

Помимо передаваемой информации, наблюдателе могут собирать некоторую информацию в момент вызова о среде выполнения и операционной системе, накапливать некоторые значения и т.д.

Для реализации предлагаются следующие варианты наблюдателей:

\begin{itemize}
\item \textbf{наблюдатель лучшего решения} — на каждой итерации сохраняет лучшее решение текущего поколения и его длину. Позволяет провести анализ того, как менялся результат работы метода;

\item \textbf{наблюдатель средних параметров} — вычисляет и сохраняет средние значения параметров $\alpha$ и $\beta$ в популяции на каждой итерации; позволяет анализировать эволюционные процессы в колонии;

\item \textbf{наблюдатель использования памяти} — собирает информацию об объёме выделенной оперативной памяти в процессе работы;

\item \textbf{наблюдатель времени выполнения} — замеряет длительность каждой итерации и общее время работы метода.
\end{itemize}

\section*{Вывод}

В результате конструкторской части были сформулированы требования к разрабатываемому методу, представлена его формализация в нотации IDEF0 и детально описаны все этапы работы: инициализация, построение маршрутов, обновление феромонов, формирование новой популяции с использованием операторов селекции, скрещивания и мутации. Выбраны и обоснованы структуры данных для реализации метода (матрица весов для графа, матрица феромонов, массив для популяции и маршрута, хеш-таблица для табу-списка). Предложена модульная архитектура, разделяющая компоненты на модули структур данных, ядра, стратегий и наблюдателей, что обеспечивает гибкость конфигурации и расширяемость метода. Разработанный метод сочетает муравьиный алгоритм с эволюцией гетерогенных параметров $\alpha$ и $\beta$ на основе генетического алгоритма, что позволяет адаптировать поведение муравьёв под конкретную конфигурацию графа.


\clearpage
